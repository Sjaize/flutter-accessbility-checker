{
  "publisher": "my-publisher",
  "name": "flutter-accessibility-checker",
  "displayName": "Flutter Accessibility Checker",
  "description": "VS Code extension for Flutter accessibility analysis and code suggestions",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:flutter-accessibility.test"
  ],
  "main": "./out/extension-minimal.js",
  "contributes": {
    "commands": [
      {
        "command": "flutter-accessibility.test",
        "title": "Test Command",
        "category": "Flutter Accessibility"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "flutter-accessibility.test",
          "when": "workspaceFolderCount > 0"
        }
      ]
    }
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}

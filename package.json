{
  "publisher": "my-publisher",
  "name": "flutter-accessibility-checker",
  "displayName": "Flutter Accessibility Checker",
  "description": "VS Code extension for Flutter accessibility analysis and code suggestions",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "nls": {
    "en": "./package.nls.json"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:flutter-accessibility.startAnalysis"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "flutter-accessibility.startAnalysis",
        "title": "%command.startAnalysis.title%",
        "category": "%category.flutterAccessibility%"
      },
      {
        "command": "flutter-accessibility.stopAnalysis",
        "title": "%command.stopAnalysis.title%",
        "category": "%category.flutterAccessibility%"
      },
      {
        "command": "flutter-accessibility.showReport",
        "title": "%command.showReport.title%",
        "category": "%category.flutterAccessibility%"
      },
      {
        "command": "flutter-accessibility.analyzeLabels",
        "title": "%command.analyzeLabels.title%",
        "category": "%category.flutterAccessibility%"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "flutter-accessibility.startAnalysis",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "flutter-accessibility.stopAnalysis",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "flutter-accessibility.showReport",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "flutter-accessibility.analyzeLabels",
          "when": "workspaceFolderCount > 0"
        }
      ]
    }
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "vscode-nls": "^5.2.0"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
